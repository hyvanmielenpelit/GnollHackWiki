---
name: wiki-verify-objects
description: Instructions for comparing regular object items (weapons, armor, tools, etc.) in GnollHackWiki with the GnollHack C code data structures (objclass) to spot mistakes in the documentation.
---

The full skill lives in this repository's tool-neutral agent directory (`.agents/`),
which is shared with other AI coding agents. This file is only a pointer.

Read `.agents/skills/wiki_verify_objects/SKILL.md` (path relative to the repository root) in full
before proceeding, and follow it. Any `references/` files it links are relative to that
same directory.